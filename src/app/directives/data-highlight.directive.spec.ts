import { DataHighlightDirective } from './data-highlight.directive';

describe('DataHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new DataHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
